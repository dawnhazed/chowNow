<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChowNow">

        <activity
            android:name=".ui.loginregister.register.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.ChowNow" />

        <activity
            android:name=".ui.loginregister.WelcomeActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ChowNow">

<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->
<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
<!--            -->
        </activity>

        <activity
            android:name=".ui.dashboard.history.detail.DetailHistoryActivity"
            android:exported="false"
            android:theme="@style/Theme.ChowNow" />

        <activity
            android:name=".ui.dashboard.geolocation.LocationActivity"
            android:exported="false"
            android:theme="@style/Theme.ChowNow" />

        <activity
            android:name=".ui.dashboard.resto.confirm.ConfirmOrderActivity"
            android:exported="false"
            android:theme="@style/Theme.ChowNow" />

        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="true"
            android:theme="@style/Theme.ChowNow" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".ui.loginregister.login.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.ChowNow" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <meta-data
            android:name="com.google.android.gms.ads.AD_MANAGER_APP"
            android:value="true" />

    </application>

</manifest>
