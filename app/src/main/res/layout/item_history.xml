<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- ImageView untuk gambar restoran -->
        <ImageView
            android:id="@+id/imageViewRestaurant"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_marginEnd="8dp"
            android:contentDescription="@string/restaurant_image"
            android:background="@android:color/darker_gray"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- Nama Restoran -->
        <TextView
            android:id="@+id/textViewRestaurantName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Nama Restoran"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@+id/imageViewRestaurant"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.0" />

        <!-- Jumlah Pesanan -->
        <TextView
            android:id="@+id/textViewOrderQuantity"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Jumlah Pesanan: 3 Makanan"
            app:layout_constraintTop_toBottomOf="@+id/textViewRestaurantName"
            app:layout_constraintStart_toEndOf="@+id/imageViewRestaurant"
            app:layout_constraintEnd_toStartOf="@+id/textViewOrderTime"
            app:layout_constraintHorizontal_bias="0.0" />

        <!-- Waktu Pesan -->
        <TextView
            android:id="@+id/textViewOrderTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Waktu Pesan: Hari ini, 17.30"
            app:layout_constraintTop_toTopOf="@+id/textViewOrderQuantity"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="@+id/textViewOrderQuantity"
            app:layout_constraintStart_toEndOf="@+id/textViewOrderQuantity"
            app:layout_constraintHorizontal_bias="1.0" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
