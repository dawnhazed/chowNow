<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- ImageView untuk gambar restoran -->
        <ImageView
            android:id="@+id/iv_resto"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_marginEnd="8dp"
            android:contentDescription="@string/restaurant_image"
            android:background="@android:color/darker_gray"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- Nama Restoran -->
        <TextView
            android:id="@+id/tv_nama_resto"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="Nama Restoran"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/iv_resto"
            app:layout_constraintEnd_toStartOf="@id/btn_detail_resto"
            app:layout_constraintHorizontal_bias="0.0" />

        <!-- Informasi Jarak dan Waktu -->
        <TextView
            android:id="@+id/tv_estimate_reach"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="2.32 km dari Anda • 7 menit"
            app:layout_constraintTop_toBottomOf="@id/tv_nama_resto"
            app:layout_constraintStart_toEndOf="@id/iv_resto"
            app:layout_constraintEnd_toStartOf="@id/btn_detail_resto"
            app:layout_constraintHorizontal_bias="0.0" />

        <!-- Rating Bintang -->
        <TextView
            android:id="@+id/tv_rating"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="4.0/5.0 ★★★★☆"
            app:layout_constraintTop_toBottomOf="@id/tv_estimate_reach"
            app:layout_constraintStart_toEndOf="@id/iv_resto" />

        <!-- Tombol Navigasi -->
        <Button
            android:id="@+id/btn_detail_resto"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@android:drawable/ic_media_play"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
